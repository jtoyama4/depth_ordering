import numpy as np
from net import Encoder, Color

if __name__ == "__main__":
    encoder = 
    criterion = nn.MSELoss()
    optimizer = torch.optim.Adam(model.parameters(), lr=learning_rate,
                             weight_decay=1e-5)
